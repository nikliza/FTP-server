#pragma once

#include <string>

namespace FtpServer
{
    class String  // Функции для строк
    {
        public:
            static std::string toUpper(std::string value);  // Сделать все буквы в строки заглавными
    };
}